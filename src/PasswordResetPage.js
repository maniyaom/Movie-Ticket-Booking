// Create a file named PasswordResetPage.js

import React from 'react';
import PasswordReset from './PasswordReset'; // Adjust the path as needed

const PasswordResetPage = () => {
    return (
        <div>
            <h2>Password Reset</h2>
            <PasswordReset />
        </div>
    );
};

export default PasswordReset;

// In your main routing component (like App.js), add the route for this page:

